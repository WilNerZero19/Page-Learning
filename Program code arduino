Program simpel 

void setup() {
  Serial.begin(9600);  // Mulai Serial Monitor dengan 9600 baud
  Serial.println("=== Arduino Siap ===");
}
void loop() {
  Serial.print("step");
  Serial.print("count");
  round
  delay(1000);
}

Program Button speedUp (Modi


#define ledRed 2     (It can be changed depending on the digitalWrite)
#define ledabang 5
#define buttonSpeedUp 7


void setup() {
  // put your setup code here, to run once:
  Serial.begin(9600);  //monitoring log input

  pinMode(buttonSpeedUp, INPUT);
  //default state = 1
  digitalWrite(buttonSpeedUp, HIGH);  // default state
  pinMode(ledRed, OUTPUT);
  pinMode(ledabang, OUTPUT);
}
void speedMode(int option) {
  if (option == 1) {  //mode kenceng
    digitalWrite(ledRed, HIGH);
    delay(100);
    digitalWrite(ledRed, LOW);
    digitalWrite(ledabang, HIGH);
    delay(100);
    digitalWrite(ledabang, LOW);

  } else {            // mode lambat
    digitalWrite(ledRed, HIGH);
    delay(300);                    (The numbers can be changed)
    digitalWrite(ledRed, LOW);
    digitalWrite(ledabang, HIGH);
    delay(300);                    (The numbers can be changed.)
    digitalWrite(ledabang, LOW);
  }
}

void loop() {
  // put your main code here, to run repeatedly:
  int isButtonSpeedPressed = digitalRead(buttonSpeedUp);
  if (isButtonSpeedPressed == 0) {  //jika tombol di pencet
     speedMode(1);
  }else {
    speedMode(0);
  }
}


Simple  progaram (pembuka)
void setup () {
Serial.begin(9600); / pinMode(LED_BUILIN, output);
}

void loop () {
digitalWrite(LED_BUILTIN, HIGH);
delay(2000);
digitalWrite(LED_BUILTIN, LOW );
delay(2000);
}

============== Code Introduction ========
chapter 1 

Fungsi 
* setup()    [pengertian  setup berfungsi hanya sekali setiap penyalan]
* loop()     [Pengertian  Loop berfungsi untuk mengulang selama arduino masih berfungsi]

  Sebagai contoh dapat di coba dengan Mengunakan Led light 
Hubungkan led light dengan kaki pendek ke GND dan kaki panjang ke no 13 
hasil nya dapat di lihat akan berfungsi (hubungkan kabel usb ke arduino terlebih dahulu.

di mana Bahan bahan2 nya * Arduino   (sebagai contoh arduino nya berfungsi )
                         * Kabel 
                         * led 
Chapter 2 

Kurung Awal () contoh Seperti void setup() and loop ()

kurung awal adalah merupakn bagian dari bahasa pemograman C++ ( fakata nya arduino di program dengan bahasa C++)
seperti void setup () {
Serial.begin(9600);    // jangan lupa pakai tanda (;) pada akhir text
}

fungsi nya untuk memeriksa sambungan keseimbangan kurung kurawal  jika tidak program tidak berfungsi / eror
kurung kurawal yang tidak seimbang sering kali dapat menyebabkan eror.




